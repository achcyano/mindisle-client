name: models
description: Shared health-domain models and formatters for MindIsle apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.2

